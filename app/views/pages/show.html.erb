<div class="container">
  <div class="row justify-content-center">
    <div class="col col-lg-10 col-xl-8">
      <div class="row header-space">
        <h1><span class="blue-word">Seu</span> Cadastro</h1>
      </div>
      <div class="row header-text">
        <p> Aqui você pode editar os dados do seu cadastro.</p>
      </div>
      <hr class="solid">
      <div class="header-text">
        <label>CPF</label>
        <label class="cpf"><%= @user.cpf %></label>
        <label>Nome</label>
        <label class="input"><%= @user.full_name %></label>
        <label>E-mail</label>
        <label class="input"><%= @user.email %></label>
        <label>Telefone</label>
        <label class="input"><%= @user.phone_number %></label>
        <label>Endereço</label>
        <label class="input"><%= @user.address %></label>
      </div>
      <div class="row justify-content-end">
        <%= link_to 'Voltar', pages_path, class: "br-button secondary mt-3 mt-sm-0" %>
        <%= link_to 'Editar', edit_page_path(@user), class: "br-button primary mt-3 mt-sm-0 mx-sm-3" %>
      </div>
    </div>
  </div>
</div>
