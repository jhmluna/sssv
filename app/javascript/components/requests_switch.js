// here we hide or not the unselected requests by its status

// document.querySelectorAll("button").forEach((button) => {
//   button.addEventListener("click", (event) => {
//     console.log(event);
//     const stat = documento.getElementByTagName("td").innerText;
//     console.log(stat);
//   });
// });

